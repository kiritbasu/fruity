{
  "name": "fruity",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "assets": "node scripts/fetch-assets.mjs",
    "dev": "npm run assets && vite",
    "build": "npm run assets && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35",
    "@vercel/analytics": "^2.0.1",
    "@vercel/functions": "^3.7.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "description": "Slice flying fruit with your bare hand. Browser hand tracking, no install.",
  "license": "MIT",
  "keywords": [
    "webrtc",
    "mediapipe",
    "hand-tracking",
    "game",
    "computer-vision",
    "vite"
  ]
}
